# Internship report drafts (local only)
internship-report/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
vitest_mistake.md
node_modules
dev-dist
dist
dist-ssr
*.local

*presentation*

ssh_key*

# Environment files - NEVER commit these!
.env
.env.local
.env.*.local
# Exception: .env.example should be committed
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Scratch / tool notes, not source. Scoped to note files so that a source
# file legitimately named Markdown.tsx is not silently untracked.
*markdown*.md
*markdown*.txt
*codex*
