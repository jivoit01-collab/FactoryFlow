# docs/ — No Tests

The `docs/` folder in the notifications module contains only documentation files (`README.md`).
These files have no executable logic and therefore no corresponding test files are needed.
