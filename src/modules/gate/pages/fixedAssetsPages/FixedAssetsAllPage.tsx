import { FIXED_ASSET_FLOW } from '../../constants/entryFlowConfig';
import SharedAllPage from '../shared/SharedAllPage';

export default function FixedAssetsAllPage() {
  return <SharedAllPage config={FIXED_ASSET_FLOW} />;
}
