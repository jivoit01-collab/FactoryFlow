import SharedAllPage from '../shared/SharedAllPage'
import { DAILY_NEED_FLOW } from '../../constants/entryFlowConfig'

export default function DailyNeedsAllPage() {
  return <SharedAllPage config={DAILY_NEED_FLOW} />
}
