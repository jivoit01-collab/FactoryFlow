import { CONSTRUCTION_FLOW } from '../../constants/entryFlowConfig';
import SharedAllPage from '../shared/SharedAllPage';

export default function ConstructionAllPage() {
  return <SharedAllPage config={CONSTRUCTION_FLOW} />;
}
